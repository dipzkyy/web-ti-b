<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/animations.css">
    <style>
        .gallery-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        .page-header {
            text-align: center;
            margin-bottom: 3rem;
            padding-top: 2rem;
        }

        .page-title {
            font-size: 3rem;
            margin-bottom: 1rem;
            background: linear-gradient(45deg, #00bfff, #0080ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .page-subtitle {
            font-size: 1.2rem;
            color: #bfefff;
        }

        .gallery-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .gallery-item {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(12px);
            border-radius: 1rem;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
            position: relative;
        }

        .gallery-item::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            border-radius: 1rem;
            padding: 2px;
            background: linear-gradient(45deg, #00bfff, #0080ff, #00bfff);
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            opacity: 0.7;
        }

        .gallery-item:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 191, 255, 0.3);
        }

        .gallery-image {
            width: 100%;
            height: 250px;
            background: linear-gradient(45deg, #1e3a8a, #0369a1);
            display: flex;
            align-items: center;
            justify-content: center;
            color: rgba(255, 255, 255, 0.8);
            font-size: 1rem;
            position: relative;
            overflow: hidden;
        }

        .gallery-image::before {
            content: "🖼️ " attr(data-title);
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
        }

        .gallery-info {
            padding: 1.5rem;
        }

        .gallery-info h3 {
            font-size: 1.3rem;
            margin-bottom: 0.5rem;
            color: #ffffff;
        }

        .gallery-info p {
            color: #bfefff;
            margin-bottom: 1rem;
            line-height: 1.5;
        }

        .gallery-date {
            color: #00bfff;
            font-size: 0.9rem;
            font-weight: 600;
        }

        @media (max-width: 768px) {
            .gallery-grid {
                grid-template-columns: 1fr;
            }
            
            .page-title {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <!-- Particle Stars Background -->
    <canvas id="particles-js"></canvas>
    
    <div class="aurora"></div>

    <main class="page-entrance">
        <div class="gallery-container">
            <!-- Tombol Back -->
            <a href="/home" class="back-button">
                ← Kembali ke Beranda
            </a>

            <div class="page-header">
                <h1 class="page-title stagger-item">🖼️ Galeri Kelas TI B</h1>
                <p class="page-subtitle stagger-item">Kumpulan moment dan kegiatan kelas</p>
            </div>
            
            <div class="gallery-grid">
                <% galeri.forEach((item, index) => { %>
                    <div class="gallery-item stagger-item">
                        <div class="gallery-image" data-title="<%= item.judul %>">
                            <!-- Placeholder untuk gambar -->
                        </div>
                        <div class="gallery-info">
                            <h3><%= item.judul %></h3>
                            <p><%= item.deskripsi %></p>
                            <div class="gallery-date">
                                📅 <%= new Date(item.date).toLocaleDateString('id-ID', { 
                                    weekday: 'long', 
                                    year: 'numeric', 
                                    month: 'long', 
                                    day: 'numeric' 
                                }) %>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <p>© 2025 TI B UNISBA Blitar — All Rights Reserved.</p>
    </footer>

    <script src="/js/app.js"></script>
    <script src="/js/particles.js"></script>
</body>
</html>